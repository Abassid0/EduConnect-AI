# API URL — defaults to "/api/v1" for local dev (proxied by Vite)
# For production (Vercel/Netlify), set to the full Railway API URL:
# VITE_API_URL=https://educonnect-ai-api.up.railway.app/api/v1
VITE_API_URL=
